const loginController = require('./login.controller');
const userController = require('./user.controller');
const clientsController = require('./client.controller');

module.exports = {
  loginController,
  userController,
  clientsController,
};
